{
  "name": "before-you-send-tests",
  "version": "0.11.2",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.cjs"
  },
  "devDependencies": {
    "@napi-rs/canvas": "0.1.100"
  }
}
